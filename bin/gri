#!/bin/sh
# gri
# Created: Fri Sep  7 11:27:10 MYT 2012
if [ -z "$1" ]; then
	exit 1;
fi

case $1 in
	[0-9][0-9])
		git rebase -i "HEAD~$1" ;;
	[0-9])
		git rebase -i "HEAD~$1" ;;
	*)
		git rebase -i "$@" ;;
esac
