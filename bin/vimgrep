#!/bin/sh
# vimgrep
# Created: Thu Apr 24 13:31:26 SGT 2014

if [ "$(uname)" = 'Darwin' ]; then
	qf_grep=/tmp/qf_vimgrep.txt
else
	qf_grep=/dev/shm/qf_vimgrep.txt
fi

trap "rm -f $qf_grep" EXIT
grep -H -n $@ > $qf_grep

vi -q $qf_grep
